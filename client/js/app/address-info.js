import { AddressInfoContainer } from '../container/address-info-container.js';

document.addEventListener('DOMContentLoaded', async () => {
    const addressInfoContainer = new AddressInfoContainer();
});

